---
title: "TP 2 - Modelado Estadístico"
author: |
  Allami Florencia - Vanotti Franco

output: 
  pdf_document:
    latex_engine: xelatex


---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(stringr)
library(tidytext)
library(forcats)
library(patchwork)
library(tidytext)

# result='hide' oculta tmb el resultado del chunk
```



```{r load_data, echo=FALSE}

# Cargar datos

titles <- read.csv("titles_train.csv",
                   stringsAsFactors = FALSE)

credits <- read.csv("credits_train.csv",
                    stringsAsFactors = FALSE)
```



```{r summary_data, echo=FALSE, results='hide'}

# Vista general

glimpse(titles)

summary(titles)

colSums(is.na(titles))

```


```{r plot_data, echo=FALSE}

ggplot(titles, aes(imdb_score)) +
    geom_histogram(bins = 30,
                   fill = "steelblue",
                   color = "white") +
    labs(title="Distribución de IMDb Score")

```



```{r generos_puntaje, echo=FALSE, results="hide"}

# (a) ¿Qué géneros parecen asociados al puntaje?

# La columna genres es una lista almacenada como texto. Hay que separarla.

genres_long <-
    titles %>%
    select(id, imdb_score, genres) %>%
    mutate(genres =
               genres %>%
               str_remove_all("\\[|\\]|'")) %>%
    separate_rows(genres, sep=",") %>%
    mutate(genres = str_trim(genres))

```


```{r promedio_por_genero, echo=FALSE, results="hide"}

# Ahora podemos calcular el promedio por género.

genre_summary <-
    genres_long %>%
    group_by(genres) %>%
    summarise(
        n = n(),
        promedio = mean(imdb_score, na.rm = TRUE)
    ) %>%
    arrange(desc(promedio))

# Mostrar sólo géneros frecuentes.

genre_summary %>%
    filter(n >= 50) %>%
    ggplot(aes(fct_reorder(genres, promedio), promedio)) +
    geom_col(fill="darkorange") +
    coord_flip() +
    labs(
        x="Género",
        y="IMDb promedio"
    )

genres_long %>%
    group_by(genres) %>%
    filter(n() >= 50) %>%
    ggplot(aes(fct_reorder(genres, imdb_score, median),
               imdb_score)) +
    geom_boxplot() +
    coord_flip()
```


```{r actores_puntaje, fig.align='center', echo=FALSE, fig.width=5, fig.height=3}

#(b) ¿Qué actores están asociados con mejores puntajes?

actors <-
    credits %>%
    filter(role == "ACTOR")

# Hacemos el merge.

actor_scores <-
    actors %>%
    inner_join(
        titles %>% select(id, imdb_score),
        by="id"
    )

# Calculamos promedio.

actor_summary <-
    actor_scores %>%
    group_by(name) %>%
    summarise(
        peliculas = n(),
        promedio = mean(imdb_score)
    ) %>%
    filter(peliculas >= 5) %>%
    arrange(desc(promedio))


# Top mejores actores.

actor_summary %>%
    slice_max(promedio, n=15) %>%
    ggplot(aes(fct_reorder(name, promedio),
               promedio)) +
    geom_col(fill="forestgreen") +
    coord_flip()

# Top peores actores

actor_summary %>%
    slice_min(promedio, n=15) %>%
    ggplot(aes(fct_reorder(name, promedio),
               promedio)) +
    geom_col(fill="firebrick") +
    coord_flip()

```




```{r directores_puntaje, echo=FALSE, results="hide"}

# Directores

directors <-
    credits %>%
    filter(role == "DIRECTOR")

director_scores <-
    directors %>%
    inner_join(
        titles %>%
        select(id, imdb_score),
        by="id"
    )

director_summary <-
    director_scores %>%
    group_by(name) %>%
    summarise(
        peliculas=n(),
        promedio=mean(imdb_score)
    ) %>%
    filter(peliculas>=3)

director_summary %>%
    slice_max(promedio,n=15) %>%
    ggplot(aes(fct_reorder(name,promedio),
               promedio))+
    geom_col(fill="steelblue")+
    coord_flip()

# Top peores actores

director_summary %>%
    slice_min(promedio,n=15) %>%
    ggplot(aes(fct_reorder(name,promedio),
               promedio))+
    geom_col(fill="lightgreen")+
    coord_flip()

```


```{r palabras_puntaje, echo=FALSE, results="hide"}

# (c) Palabras asociadas con el puntaje

words <-
    titles %>%
    select(id, imdb_score, description) %>%
    unnest_tokens(word, description)

data(stop_words)

words <-
    words %>%
    anti_join(stop_words, by="word")

word_summary <-
    words %>%
    group_by(word) %>%
    summarise(
        frecuencia = n(),
        promedio = mean(imdb_score)
    ) %>%
    filter(frecuencia >= 20)

word_summary %>%
    slice_max(promedio, n=20) %>%
    ggplot(aes(fct_reorder(word,promedio),
               promedio))+
    geom_col(fill="darkgreen")+
    coord_flip()

word_summary %>%
    slice_min(promedio, n=20) %>%
    ggplot(aes(fct_reorder(word,promedio),
               promedio))+
    geom_col(fill="red")+
    coord_flip()
```



```{r palabras_titulo_puntaje, echo=FALSE, results="hide"}

# Palabras del título

title_words <-
    titles %>%
    select(imdb_score, title) %>%
    unnest_tokens(word, title) %>%
    anti_join(stop_words)

title_summary <-
    title_words %>%
    group_by(word) %>%
    summarise(
        frecuencia=n(),
        promedio=mean(imdb_score)
    ) %>%
    filter(frecuencia>=10)


```

2. Causalidad. Se tiene el siguiente DAG: 

[img]

donde Comedia es una variable binaria que indica si el género del título incluye comedia. ¿A
qué subconjunto de las variables Año, Duración y País se debe condicionar para estimar
el efecto causal directo de Comedia sobre el Score —es decir, el efecto que no está
mediado por la Duración—? Dar todas las posibilidades y justificar. Luego estimar ese
efecto con una regresión sobre los datos (Comedia sale de genres, Duración de runtime,
Año de release_year y País de production_countries) y compararlo con la asociación
cruda de Comedia sobre Score (sin ajustar por nada).

El efecto de Comedia sobre Score está mediado por Duración, que crea un camino siendo cadena. Además, está afectado por País, que crea un camino como cofounder. Al condicionar por Duración, que es collider entre Año y Comedia, se abre el camino Comedia - Duración - Año - Score, por lo que hay que también condicionar por Año. Luego, la forma más clara de medir el efecto causal directo de Comedia sobre Score es condicionando por Duración, País y Año.

Para medir el efecto causal promedio, basta medir la asociación, gracias al criterio de la puerta trasera. Bloquear los caminos de acción indirecta permite que el valor real de Comedia y el Score que obtendría dado un valor de Comedia sean independientes, y por lo tanto, la asociación y el efecto causal son iguales. 


```{r efecto_causal_directo, echo=FALSE, results="hide"}

# acá entiendo que hay que hacer la esperanza sobre nuestras condicionales, de la esperanza de Score dado comedia = 1 y condicionales = z menos la esperanza de Score dado comedia = 0 y condicionales = z.



```


3. Selección de modelos. Dividir el conjunto de datos en entrenamiento y testeo (tam
bién pueden usar otra técnica, como validación cruzada). Con todas las variables dispo
nibles, probar al menos 5 modelos diferentes y mostrar la estimación del error
cuadrático medio (MSE) de cada uno. Entre los modelos candidatos deben incluir sí o
sí al menos un modelo de efectos mixtos y una regresión con splines. Elegir el modelo
que minimice el MSE de predicción para el rating de IMDB.

Cinco modelos: efectos mixtos, regresión con splines, lineal?, uhh bayes (previa semi normal)?, idk??

```{r modelos, echo=FALSE, results="hide"}

# implementar modelo (?) por ahí toca definir cosas con STAN.

```

4. Competencia. En los archivos titles_test.csv y credits_test.csv aparecen
1652 nuevos títulos, para los cuales no se incluye el rating de IMDB (pero yo sí
lo tengo). A partir del modelo elegido en el ítem anterior, producir un archivo
predicciones_<apellido1>_<apellido2>_<apellido3>.csv con una sola columna
y sin encabezado, donde la fila i contenga la predicción del rating de IMDB para el
título de la fila i (debe tener tantas filas como el test). A partir de esas predicciones, yo
voy a computar el error cuadrático medio de cada equipo, y gana el que tenga el menor.

```{r nuestras_predicciones, echo=FALSE, results="hide"}

# hacer las predicciones

```

